import 'package:flutter_test/flutter_test.dart';
import 'package:trailwire/main.dart';

void main() {
  test('TrailwireApp instantiates successfully', () {
    const app = TrailwireApp();
    expect(app, isNotNull);
  });
}
